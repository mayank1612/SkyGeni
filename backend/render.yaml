# Render deployment configuration
services:
  - type: web
    name: skygeni-backend
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
